#include <stdlib.h>
#include <stdio.h>

void addition(char selection) {
	if(selection == 'A') {
		int x, y, result;
		cout << "Insert the number: \nValue: ";
		scanf("%d", x);
	} else if(selection == 'B') {
		
	}
	cout << "The result for: " << x << " + " << y << " is " << x + y;
}
